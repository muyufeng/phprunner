# ============================================================
# 构建密钥模板 —— 复制为 build/secrets.env 后填入真实值
#
# 用法（WSL 内执行）：
#   cp build/secrets.env.example build/secrets.env
#   编辑 build/secrets.env，粘贴你的 token
#
# ⚠️ build/secrets.env 已在 .gitignore——真实文件永不入库。
#    本模板可以入库（不含任何真实值）。
#
# 消费方：根 Makefile 的 frankenphp 目标（构建前 source），
#        经 --build-arg 注入构建容器，用于规避 GitHub API 匿名限额
#        （60/h → 5000/h）。
# ============================================================

# 临时构建 token（建议：fine-grained PAT、零权限、短有效期、用后撤销）
GITHUB_TOKEN=
