; phprunner conf.d 分片目录（经镜像 ENV PHP_INI_SCAN_DIR=/etc/php/conf.d 启用）
; 本文件为运行时生成槽位：entrypoint.d/10-assemble-config.sh 在
; LOG_OUTPUT_LEVEL=debug 时写入错误显示翻转（display_errors 等），
; 非 debug 时删除本文件。zzz- 前缀保证最后加载、覆盖主模板。
; 用户手工挂载其他 *.ini 分片即自动生效（M2-08 实证）。
