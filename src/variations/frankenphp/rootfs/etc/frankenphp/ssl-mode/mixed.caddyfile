# SSL_MODE=mixed：HTTP + HTTPS 并存
{$CADDY_HTTP_SERVER_ADDRESS:http://} {
	import php-app-common
}

{$CADDY_HTTPS_SERVER_ADDRESS:https://} {
	import tls-{$CADDY_AUTO_HTTPS:off}
	import php-app-common
	import security-https
}
